#!/bin/bash

# Using more command to display all the .txt files on the terminal.
# I decided to use the more command because it's super useeful and
# appreciatable.

more -d info.txt
more -d ./$X/dmaster/master.txt
more -d ./$X/dup/up.txt
more -d ./$X/dnp/np.txt
more -d ./$X/dsocket/socket.txt
more -d ./$X/dcb/cb.txt
